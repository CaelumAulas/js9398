Arquivos:
    * js/contatos.js : Gerencia a lista de contatos em memória (moduloContatos)
    * js/tabela.js : Exibe as informações presentes na lista de contatos na interface (moduloTabela)
    * js/formulario.js : Gerencia o formulário da interface (coleta dados e os valida antes de inserir na lista de contatos)


Métodos Recomendados para o Módulo "contatos.js":
    * adicionarContato(nome, telefone) : função que recebe o nome e o telefone coletados no formulário, os repassa para o objeto "infoContato" e o adiciona à lista de contatos 
    * getContatos(): função que retorna toda a lista de contatos em memória