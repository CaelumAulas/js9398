1) Guardar as referências dos elementos da interface usando os recursos da DOM API (document.querySelector)
2) Usar EVENT DELEGATION para remoção de itens da agenda
    => Evento click adicionado a tabela onde monitoramos se quem foi clicado foi o botão "Excluir"

3) Criar o método de remoção de itens no módulo correto (contatos.js)
4) Persistir as informações usando Storage API (LocalStorage)